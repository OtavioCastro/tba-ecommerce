CREATE TABLE IF NOT EXISTS PRODUTO(
  ID                        BIGINT PRIMARY KEY,
  NOME                      VARCHAR,
  QUANTIDADE                INT,
  VALOR                     DOUBLE,
  CATEGORIA                 VARCHAR
);